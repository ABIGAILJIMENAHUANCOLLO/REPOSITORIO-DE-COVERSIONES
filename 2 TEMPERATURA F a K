#include <iostream>

using namespace std;

int main() {
    int f, k;
    cout << "Ingrese un valor en f:";
    cin >> f;

    // Convert Fahrenheit to Kelvin
    k = (f - 32) / 1.8 + 273.15; 

    cout << "El valor en k es: " << k << endl; 

    return 0;
}
